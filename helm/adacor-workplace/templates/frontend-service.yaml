apiVersion: v1
kind: Service
metadata:
  name: {{ include "adacor-workplace.frontend.fullname" . }}
  labels:
    {{- include "adacor-workplace.frontend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adacor-workplace.frontend.selectorLabels" . | nindent 4 }}
