{{- if .Values.mcpRunner.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-platform.mcpRunner.fullname" . }}
  labels:
    {{- include "agent-platform.mcpRunner.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-platform.mcpRunner.selectorLabels" . | nindent 4 }}
{{- end }}
