apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-platform.frontend.fullname" . }}
  labels:
    {{- include "agent-platform.frontend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-platform.frontend.selectorLabels" . | nindent 4 }}
