# Docker-spezifische Overrides (optional)
# Kopiere diese Datei nach docker-compose.env und passe die Werte an.
# Diese Werte überschreiben die gleichnamigen aus .env
# PORT=3001
# TRUST_PROXY=true
# FRONTEND_URL=http://frontend:80
